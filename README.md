# htmlstrip
Strip HTML tags and extract text, links, or images.
```bash
python htmlstrip.py page.html
python htmlstrip.py https://example.com --links
python htmlstrip.py index.html --json
cat email.html | python htmlstrip.py
```
## Zero dependencies. Python 3.6+.
